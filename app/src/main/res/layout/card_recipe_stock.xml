<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:layout_margin="5dp"
    app:cardBackgroundColor="@color/card_color"
    app:cardCornerRadius="20dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:padding="10dp">

        <TextView
            android:id="@+id/r_stock_item_title"
            android:layout_width="210dp"
            android:layout_height="wrap_content"
            android:maxWidth="210dp"
            android:text="@string/dummy_text"
            android:textColor="@color/appBars_color"
            android:textSize="20sp"
            app:layout_constraintBottom_toTopOf="@+id/r_stock_item_amount"
            app:layout_constraintEnd_toStartOf="@+id/r_stock_add_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/r_stock_item_amount"
            android:layout_width="210dp"
            android:layout_height="wrap_content"
            android:maxWidth="210dp"
            android:text="@string/dummy_text"
            android:textColor="@color/appBars_color"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/r_stock_delete_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/r_stock_item_title" />

        <ImageButton
            android:id="@+id/r_stock_add_button"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:contentDescription="@string/add_button"
            android:src="@drawable/ic_add"
            app:backgroundTint="@color/card_color"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/r_stock_delete_button"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:contentDescription="@string/delete_button"
            android:src="@drawable/ic_close"
            app:backgroundTint="@color/card_color"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/r_stock_add_button" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>